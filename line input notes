turn off line wrapping
output char from start position until end of line reached.
  store cursor position of curent edit pos
work out how many characters

if inputted character is too close to the screen edge, increment start pos and redisplay. clear until end of line if at the end of the edit line


line wrapping
double wide characters

disable line wrapping. if wrapping would have caused the screen to scroll, briefly re-enable it, scroll the screen 1 line (and alter the start line) then disable it again
